def topSimilar(unknown_word, match_words_train_list, knn):
  reslut = []
  
  return result

def replace_similar_word(similar_word, word_embedding_list, test_vector_dct)
  updated_test_vector_dct = {}
  
  return updated_test_vector_dct

def bean_search():
  for i in range(len(unk_word_list)):
    similar_list = topSimilar(unk_word_list[i], ...)
    for similar_word in similar_list:
      updated_test_vector_dct = replace_similar_word(similar_word, ...)

if __name__ == '__main__':
  